server:
  port: 8080

spring:
  application:
    name: smart-order-backend
  datasource:
    url: ${DB_URL:jdbc:postgresql://db.ebuetkgmydvjrtdqkcvv.supabase.co:5432/postgres}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:Nihara@2001S}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false

jwt:
  secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
  expiration: 86400000
  refresh-expiration: 604800000

ml:
  service:
    url: ${ML_SERVICE_URL:http://localhost:8000}

logging:
  level:
    com.dartcodes.smartorder: INFO
